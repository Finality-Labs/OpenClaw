{
  "name": "finality-agent-network",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/intake",
    "packages/negotiate",
    "packages/chain",
    "packages/skill",
    "packages/reference-agent",
    "packages/orchestrator"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "goat:probe": "tsx --env-file=./.env scripts/probe.ts",
    "goat:preflight": "tsx --env-file=./.env scripts/preflight.ts",
    "goat:live": "tsx --env-file=./.env scripts/live-test.ts",
    "goat:dashboard": "tsx --env-file=./.env src/serve-dashboard.ts"
  }
}
